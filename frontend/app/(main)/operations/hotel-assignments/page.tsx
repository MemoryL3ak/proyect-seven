import ResourceScreen from "@/components/ResourceScreen";
import { resources } from "@/lib/resources";

export default function HotelAssignmentsPage() {
  return <ResourceScreen config={resources.hotelAssignments} />;
}
