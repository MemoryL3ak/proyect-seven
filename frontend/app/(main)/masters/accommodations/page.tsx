import ResourceScreen from "@/components/ResourceScreen";
import { resources } from "@/lib/resources";

export default function AccommodationsPage() {
  return <ResourceScreen config={resources.accommodations} />;
}
