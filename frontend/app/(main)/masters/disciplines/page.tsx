import ResourceScreen from "@/components/ResourceScreen";
import { resources } from "@/lib/resources";

export default function DisciplinesPage() {
  return <ResourceScreen config={resources.disciplines} />;
}
